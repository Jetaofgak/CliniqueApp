{% extends 'base.html.twig' %}

{% block title %}Welcome to the Clinique{% endblock %}

{% block body %}
<head>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <style>
        body {
            background: #ffffff; /* White background */
            font-family: "Poppins", sans-serif;
            margin: 0;
            padding: 0;
        }

        /* Header styling (same as Room List header) */
        .table-container {
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 8px;
            background-color: #ffffff; /* White background */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Light shadow for depth */
            margin-bottom: 20px; /* Space at the bottom */
        }

        h1 {
            font-size: 36px;
            font-weight: bold;
            color: #333;
            text-align: center;
            margin-bottom: 20px; /* Space below the header */
        }
    </style>
</head>
<body>
    <div class="container">
        <a href="{{ path('app_rooms') }}"><button class="nav-button">View Rooms</button></a>
        <a href="{{ path('reservation_index') }}"><button class="nav-button">Reservations</button></a>
        <a href="{{ path('schedule_index') }}"><button class="nav-button">Schedule</button></a>
        <a href="{{ path('app_register') }}"><button class="nav-button">Register</button></a>
        <a href="{{ path('app_login') }}"><button class="nav-button">Login</button></a>
    </div>
</body>
{% endblock %}
